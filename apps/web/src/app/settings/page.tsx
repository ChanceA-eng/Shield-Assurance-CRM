import SectionLayout from '../../components/crm/SectionLayout';

export default function SettingsPage(): JSX.Element {
  return (
    <SectionLayout title="Settings">
      <p className="text-sm text-[#3e3e3c]">Settings module route is active.</p>
    </SectionLayout>
  );
}
